---
git_repo: <git repo>
git_version: <This can be the git SHA, the literal string HEAD, a branch name, or a tag name>
ivs_grading_server_url: <base url to the grading server>
ivs_edx_shared_key: <secret key used to sign cookies with>
ivs_debug: <set to '' to turn off debug>
mws_debug: <set to '' to turn off debug>
owner: <source code owner user>
shared_secret_key: <secret key for secure cookie signing for ivs and mws>
uwsgi_uid: <uwsgi will run as this user>
